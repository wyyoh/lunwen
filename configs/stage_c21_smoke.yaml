experiment:
  feature_cache: artifacts/stage_c2/canonicalizer_features.pt
  r0_checkpoint: artifacts/stage_c2/Q3/canonicalizer.pt
  public_confirmation_seal: artifacts/stage_c21/confirmation_seal.json

training:
  seed: 0
  steps: 2
  query_dim: 16
  mlp_hidden_size: 32
  dropout: 0.0
  temperature: 0.07
  batch_facts: 4
  templates_per_fact: 2
  learning_rate: 0.001
  weight_decay: 0.01
  warmup_fraction: 0.0
  evaluation_interval: 2
  relation_pretrain_fraction: 0.50

evaluation:
  batch_size: 128
  ridge_strength: 0.01
  template_probe_seed: 91
